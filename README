A test application.

Models:

* SSHKey (ID, Body)

Operations:

* SSHKey CRUD
* SSHKey deploy to AWS/EC2

It is written in:

* Chi (middleware, render)
* SQLBoiler (sqlite)
* AWS SDK v2
* Prometheus go client
* zerolog/CloudWatch

This is a PUBLIC DOMAIN.
